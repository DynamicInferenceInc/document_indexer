{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "functional_direction": {
      "type": ["string", "null"],
      "description": "Функциональное направление из роли на проекте, иначе из выполненных работ. Пример: роль «Консультант по направлению Казначейство» → «Казначейство». Не выдумывай. Если нельзя выделить — null."
    }
  },
  "required": ["functional_direction"]
}
