{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "functional_direction": {
      "type": ["string", "null"],
      "description": "Функциональное направление: блок, модуль или предметная область из роли, иначе из выполненных работ, иначе из должности в шапке. Не грейд. Пример: «Консультант по направлению Казначейство» → «Казначейство». Не выдумывай. Если нельзя выделить — null."
    }
  },
  "required": ["functional_direction"]
}
