Ниже полный текст резюме и список проектов, уже извлечённых из него (с номером index).
Для каждого проекта сделай две вещи.

A) Дозаполни ТОЛЬКО те поля, которые перечислены для него как пустые, дословными формулировками из текста резюме.
   - Если в тексте нет данных для поля — null.
   - Уже заполненные поля не меняй и не переписывай.
   - Не переноси значения из другого проекта, если в тексте не сказано, что они относятся к этому проекту.
   - work_performed — пункты через «; ».
   - Ничего не выводи из общих знаний, не сокращай и не перефразируй.

B) Определи functional_direction и solution_platform по правилам ниже.

Верни только JSON по схеме: массив projects, каждый элемент содержит index исходного проекта. Не выдумывай.

Правила для functional_direction и solution_platform:
