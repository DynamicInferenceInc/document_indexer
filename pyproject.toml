[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "document-indexer"
version = "0.1.0"
description = "Importable document indexer: Docling chunking, Qdrant vectors, local or SMB sources"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "httpx>=0.27.0",
    "pydantic-settings>=2.0.0",
    "qdrant-client>=1.9.0",
]

[project.optional-dependencies]
docling = [
    "docling>=2.122.0",
]
runtime = [
    "watchdog>=4.0.0",
]
smb = [
    "smbprotocol>=1.13.0",
]
dev = [
    "pytest>=8.0.0",
]
all = [
    "document-indexer[docling,runtime,smb]",
]

[tool.hatch.build.targets.wheel]
packages = ["src/document_indexer"]

[tool.hatch.build.targets.wheel.force-include]
"src/document_indexer/resume/prompt.txt" = "document_indexer/resume/prompt.txt"
"src/document_indexer/resume/prompt_projects.txt" = "document_indexer/resume/prompt_projects.txt"
"src/document_indexer/resume/prompt_refine.txt" = "document_indexer/resume/prompt_refine.txt"
"src/document_indexer/resume/prompt_experience.txt" = "document_indexer/resume/prompt_experience.txt"
"src/document_indexer/resume/schema_projects.json" = "document_indexer/resume/schema_projects.json"
"src/document_indexer/resume/schema_experience.json" = "document_indexer/resume/schema_experience.json"
"src/document_indexer/resume/sample.md" = "document_indexer/resume/sample.md"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
markers = [
    "smb_integration: live SMB share test (set DOCUMENT_INDEXER_SMB_TEST=1)",
]
